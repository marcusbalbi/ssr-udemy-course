import React from "react";

const Home = () => {
  return (
    <div>
      <div>teste</div>
      <button onClick={() => { console.log("ok!"); }} >press me!</button>
    </div>
  );
};

export default Home;
